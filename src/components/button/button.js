import React from 'react';
import './button.css';

const Button = ( ) => {
    return (
    <button className="section__button addButton" type="button">Добавить квест</button>
    )
}

export default Button;
